#include "main.h"

/**
 * _isdigit - checks for a digit (0 through 9)
 * @c: the character being checked
 * Return: 1 if digit else 0
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= 9)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
